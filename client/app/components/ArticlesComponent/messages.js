/*
 * ArticlesComponent Messages
 *
 * This contains all the text for the ArticlesComponent component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.ArticlesComponent';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ArticlesComponent component!',
  },
});
