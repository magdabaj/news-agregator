/*
 * SortByTag Messages
 *
 * This contains all the text for the SortByTag component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.SortByTag';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the SortByTag component!',
  },
});
