/*
 * UsersList Messages
 *
 * This contains all the text for the UsersList component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.UsersList';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the UsersList component!',
  },
});
