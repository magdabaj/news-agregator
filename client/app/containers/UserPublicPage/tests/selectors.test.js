// import { selectUserPublicPageDomain } from '../selectors';

describe('selectUserPublicPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
