/*
 * UserPublicPage Messages
 *
 * This contains all the text for the UserPublicPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.UserPublicPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the UserPublicPage container!',
  },
});
